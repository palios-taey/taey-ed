# Taey-Ed REQUIREMENTS — The Canon
**Status: v1 (2026-07-09)** — curated from FIVE independent discovery lanes: codex verbatim evidence sweep (711 lines), as-built escalation reconstruction (9-state machine, file:line-cited), git-history ruling mine (577 commits, all branches), chat/transcript mine (565 Jesse-authored messages + all 30 memory files), and the on-disk doc-corpus deep read (INTENDED_FLOW, code.bak specs, UNIVERSAL_LAYER, LAUNCH_PLAN, escalation templates, live packets). Conflicts adjudicated chronology-latest-wins with citations; unresolved tensions live in §12. Ratified under Jesse's 2026-07-09 "nothing is my call" delegation. This document is **the contract**: every phase of `taey-ed-state-context` (and any future change) is validated against it. Conflicts between code and this canon are bugs in the code. Source reports: `dispatches/discovery2/`.

Sources are cited per rule: `[CLAUDE.md]` = project instructions; `[mem:<slug>]` = project memory; `[yaml:<screen_type>]` = screen-type recipe contract; `[J:<date>]` = direct Jesse directive.

---

## 0. Mission & scale
- **R0.1** The system autonomously completes courses on educational platforms: navigate, watch, read, solve, advance — with minimal user action. `[CLAUDE.md]`
- **R0.2 SCALE:** must support **100s–1000s of platforms, figured out dynamically**. Zero per-platform code anywhere in the flow; a new platform costs one knowledge-research cycle, no code or schema changes. `[J:2026-07-09]`
- **R0.3** All intelligence is server-side and platform-generic: the model classifies, the model builds behavior trees, recipes + knowledge are data, never code. No hardcoded screen types, no platform-specific logic in the flow. (CLAUDE.md's "Gemini decides" wording is stale — the worker LLM is Claude since the June cutover; the PRINCIPLE — model decides, not code — is unchanged.) `[CLAUDE.md Cardinal Rule 1; git-history mine 2026-07-09]`
- **R0.4** No thresholds in tree analysis: presence only, never quantity heuristics — classification is the model's job. `[CLAUDE.md Cardinal Rule 2]`
- **R0.5 North star: any-LMS.** "Khan is the hard test environment, not the target. The target is any-LMS-works." Any platform a user signs up for must work. `[UNIVERSAL_LAYER_v1 §15; LAUNCH_PLAN:37]`
- **R0.6 UX over implementation; failures must feel honest.** When a trade-off appears between user UX and implementation complexity, favor UX — backend complexity is acceptable, user friction is not. The churn risk is trust collapse: failures must feel honest, calm, and recoverable, never invisible or dangerous. `[LAUNCH_PLAN:9-11, J:2026-05-11]`
- **R0.7 Product honesty.** No benchmarked speed/accuracy claims; the system stops and escalates rather than guessing; user privacy stance: no reporting of user activity to institutions, no institutional opt-outs, no enforcing institutional definitions of use. `[TAEY_ED_MANIFESTO; ETHICAL_USE:149-181]`

## 1. Architecture hard lines
- **R1.1 The Mac app is FROZEN — dumb primitives only** (capture tree + screenshot, execute BT actions, report results). **The Mac never decides. Ever. On any platform.** It is never modified to work around a server-side gap; every fix works with the tree the Mac already sends, which contains every distinguishing feature of every screen. The ONLY unfreeze path is the SHIP-LOCK discipline gate — "real run, identified gap, minimal addition," then re-lock (used once, 2026-05-08, for the Khan widget primitives). `[J:2026-07-03/05-12; app/CLAUDE.md:44-57, 92-99; UNIVERSAL_LAYER_v1:501-503]`
- **R1.1a The wire protocol is a preserved contract.** `POST /next_action {session_id, platform, tree, screenshot_b64?, client_state, last_result, chat_message, relevant_kb_chunks}` survives every rebuild — the frozen Mac defines it. **Schema boundaries must REJECT unknown-field mismatches loudly, never silently drop them**: grounding once died invisibly because GenerateRequest lacked the kb-chunks field and FastAPI silently discarded what the Mac sent (root cause 675d2d4). `[UNIVERSAL_LAYER_v1:32-49; INTENDED_FLOW_ALIGNMENT_PLAN:25]`
- **R1.2** The user's KB (content + vectors) lives on the user's Mac (sqlite+faiss) and **never leaves it**. Server sees only: text→embed requests, and a question's top-K already-retrieved chunks on a single consultation. `[CLAUDE.md hard line, J:2026-05-12]`
- **R1.3** The Mac never touches server-side databases; the wire contract is HTTP (`/next_action`, `/api/v1/embed`) only. `[CLAUDE.md]`
- **R1.4** Server state must be durable and transactional: no coordination via /tmp flag files, no failure cached as success, no content-hash as identity, no silent state loss on restart. `[J:2026-07-09; taey-ed-state-context plan]`

## 2. Screen model
- **R2.1** Six universal screen categories (NAVIGATION, VIDEO, ARTICLE, EXERCISE, TRANSITION, UNKNOWN); variants/subtypes extend them (e.g. `EXERCISE_DROPDOWN`). `[CLAUDE.md]`
- **R2.2 Determinism per category (Jesse ruling):** ARTICLE + VIDEO subtypes are FULLY automated — fixed BT, no validation. NAVIGATION subtypes share ONE fixed BT (structure constant; the send_to_llm node adapts per screen at execution). EXERCISE subtypes are NOT auto-trusted — they carry a RECIPE (the canonical BT skeleton the worker fills with question-specific values only, **never redesigns**) and need the 3-success promotion. `[_SCHEMA.md:28-32, 51-52 — supersedes the older PROMOTION_MATH NAVIGATION NO_CACHE rule]`
- **R2.2a Classification follows the decision order:** video player → article (reading vs complete by markers) → navigation (course/unit/lesson scope) → transition (intro vs summary) → exercise subtype by answer widgets → UNKNOWN. When in doubt, UNKNOWN — NEVER guess. `[_UNKNOWN_GUIDE.md:38-47; UNIVERSAL_LAYER_v1:89]`
- **R2.3** Deterministic live-widget reads outrank cache, hint, and LLM opinion when inferring an exercise subtype (the widget in the tree is ground truth). `[mem:skeleton_maxdepth_truncates_widgets]`
- **R2.4** Navigation screens stay **LLM-driven** — tighten prompts/handler shape, never replace with server-side rule resolvers. `[mem:llm_required_for_nav_screens, J:2026-05-19]`
- **R2.5** A failed or UNKNOWN classification is never terminal truth: it must remain re-queueable (bounded), and must never be served as a completed result. `[mem:classify_cache_unknown_trap]`
- **R2.6** Screen identity must be resolvable without silent fusion: if two different screens produce the same fingerprint, the system must represent the ambiguity and disambiguate — never silently treat them as one. `[mem:shared_skeleton_quiz_advance_and_label; taey-ed-state-context p3]`
- **R2.7 No bare masters.** A master category (EXERCISE, NAVIGATION, VIDEO, ARTICLE, TRANSITION) is a routing namespace, NEVER a screen: not a classify output, not a recipe/BT key, not a cached identity. Real screens are subtypes. `[_SCHEMA.md:16; _UNKNOWN_GUIDE.md:4; variant_cache bare-master refusal]`
- **R2.8 Navigation selection order:** pick the FIRST INCOMPLETE item in curriculum order (videos/articles before the lesson's exercise); completed pieces ("completed Video/Article" markers) are skipped, never re-entered; quizzes/unit tests are entered ONLY when no other incomplete content remains; "Take ... again"/mastery-marketing controls are never targets. The Up-next control's ROLE varies (AXLink vs AXButton) — read the exact name + role from the current tree, never assume. `[yaml:NAVIGATION__*, ARTICLE__COMPLETE; consultations/REVIEWS tier2 61e95ee]`

## 3. Exercise discipline
- **R3.1 ONE TRY ONLY — the ban is on SAME-ANSWER retries.** Stuck (screen unchanged after action) or wrong answer = full stop + escalate; no silent retries, no reflex re-pick/resubmit of the same answer. **Corrected-answer resubmit after a clearing action is REQUIRED** when the platform allows it (clearing mechanism is platform data — e.g. Khan = browser refresh): once the ladder determines the correct answer, the system clears and submits it; it never abandons a solvable screen. `[CLAUDE.md Cardinal Rule 3; T:2026-05-18T18:55Z, 2026-05-19T09:49Z; INTENDED_FLOW §D 2026-06-14 — adjudicated 2026-07-09: reflex-resubmit banned, ladder-corrected resubmit mandated]`
- **R3.2 Never SKIP work.** "Skip" buttons are banned. "Up Next" is banned **as a skip** — but clicking "Up Next" is CORRECT when it is the navigation to the next item AND the current screen shows a completion indicator (exercise solved / video complete). Completion first, then advance by whatever control the platform offers. `[CLAUDE.md Cardinal Rule 4, corrected J:2026-07-09]`
- **R3.3 Question context is captured on every exercise**: the question (verbatim), the enumerated options, the chosen answer, and correctness — persisted (store_qa / qa_captures) so solved screens are never re-derived from scratch. `[J:2026-07-09; yaml:* store_qa steps]`
- **R3.4 Enumerate before ask:** when options live in a widget (dropdown menus), open + enumerate them and pass the exact option set to the LLM; the answer must be selected from the enumerated set, never free-generated. `[yaml:EXERCISE_DROPDOWN contracts]`
- **R3.5** Answer widgets are targeted by element reference + position, never by name/label when names are identical. `[yaml:EXERCISE_DROPDOWN contracts]`
- **R3.6** The extraction BT pulls the real question + full structure from the content pane — never page title/nav chrome. `[mem:capture_main_pane_not_chrome]`
- **R3.7** Wrong-answer detection: same exercise screen (same skeleton) after submit = wrong answer or nothing staged — never success. `[yaml:* verification]`
- **R3.8 Content extraction is half the job.** Every video and article has its instructional substance (not chrome) extracted and stored to the user's local KB as part of handling that screen. "A run that navigates perfectly but stores no content has failed half the job." `[INTENDED_FLOW.md:41-45; yaml:VIDEO__PLAYER extract config; yaml:ARTICLE__READING "the extract is the point"]`
- **R3.9 Answers are grounded in the course's own content.** When the course asks a question, the Mac's local KB is queried by vector similarity and the top-K chunks ride the consultation; the answer is grounded in what the course taught, not general knowledge. `[INTENDED_FLOW.md:41; CLAUDE.md KB wire contract]`
- **R3.10 Partial submission is recoverable, NOT a wrong answer** — but ONLY when the platform's modal explicitly says so ("almost there / still more parts") AND no red/incorrect feedback is shown. `[yaml:EXERCISE_TABLE_INPUT:34]`
- **R3.11 Stage → verify → submit.** Staged state is verified from the CURRENT capture before Check: re-extract widget state (point names, chosen values) and confirm it matches the target; choices/checkboxes TOGGLE, so re-read before clicking. Never assume an action landed. `[yaml:EXERCISE_GRAPH_POINTS:34, EXERCISE_MULTIPLE_*]`
- **R3.12 Accuracy floor: 90%+, user-configurable.** The system's answer accuracy target is 90%+ by default; the user may configure it downward, never the system silently. `[T:2026-05-14T11:48Z]`
- **R3.13 Wrong-answer RCA audits the served context.** Every wrong-answer root-cause includes what KB/context was actually provided to the LLM for that question — a wrong answer with missing context is a context-pipeline defect, not a model defect. `[T:2026-05-20T12:44Z]`
- **R3.14 Two-step extract→solve, everywhere content is embedded.** ALL hidden/embedded content (transcripts, option internals, image descriptions) is extracted BEFORE the solve call, for every question type. Questions are stored as CONTENT, never as hashes. Course-content injection happens ONLY on question screens. `[T:2026-05-18T15:57/19:22Z, 2026-06-01T16:56Z; INTENDED_FLOW §C]`

## 4. Image / vision escalation ladder
- **R4.1 Tier 0 — the tree, free:** figures often carry the exact answer in AXImage alt-text; read the tree FIRST. `[mem:alttext_tier0_supersedes_grid_cv]`
- **R4.2 Tier 1 — screenshot vision:** solve calls attach the screenshot; labels rendered as images/MathML (absent from AX text) are read from the screenshot by position. `[yaml:EXERCISE_DROPDOWN contracts]`
- **R4.3 Tier 2 — CV/grid measurement:** computer-vision measurement (grid CV etc.) only as the alt-text-absent fallback, never as the first move. `[mem:alttext_tier0_supersedes_grid_cv]`
- **R4.4 Screenshot-first diagnosis:** on every escalation, the screenshot is ground truth and is read before any log/tree/hash reasoning. `[mem:look_at_screenshot_first, J:2026-06-01]`
- **R4.5 Image description in DISCOVER, before solve.** Image-dependent questions require a full image description in context, produced in the discovery/extraction phase BEFORE the solve call — never mid-solve improvisation, never eyeball-guessing a hard figure. `[_universal.json:32; yaml:EXERCISE_TEXT_INPUT:59; J:2026-06-15]`
- **R4.6 First-look image needs-assessment on EVERY exercise, every platform.** If images are answer-relevant, describe them in detail — as important as option enumeration. Relevance-first filter (decorative/'Related content' excluded); images INSIDE enumerated options are read too; complex-ENTER types (sorter/matcher/graph/label) are exempt from the injection because the answer must still be entered. `[T:2026-05-20T12:44Z; OP T:2026-06-15T14:50Z; mem:sorter exemption ae88972]`
- **R4.7 Image escalations follow the SAME ladder** — "same escalation process but for images"; there is no separate image escalation authority. `[OP T:2026-06-15T15:02Z]`

## 5. Video / article semantics
- **R5.1** Completion indicators gate the advance — never player presence, never "something moved". A playing state is not success. `[commit 78810a9; mem:video_complete_is_up_next_present]`
- **R5.2** "Up next" link present = the video is DONE → advance; never re-play a completed video. `[mem:video_complete_is_up_next_present, J:2026-06-02]`
- **R5.3** The completion check runs every poll cycle (a finished video's tree hash freezes; a check gated on tree-change never fires). `[commit 832a397]`
- **R5.4** `video_poll` must be the only child in its sequence. `[CLAUDE.md Cardinal Rule 5]`
- **R5.5** Completion indicators are platform DATA (recipe YAML / knowledge), not literals in route code. `[CLAUDE.md Cardinal Rule 1; taey-ed-state-context p5-declarative]`
- **R5.6 Video polling is LOCAL.** The Mac polls the player locally and contacts the server only on state change/completion; the server-side completion check runs on every poll it RECEIVES (R5.3). Target end-state: fully-automated video handling with no LLM in the loop. First completion-signal on a new platform goes to the operator for mapping. `[T:2026-05-15T15:09/15:14Z, 2026-06-01T18:43Z]`
- **R5.7 Articles: scroll gradually to bottom + extract before advancing — universal.** An article map without scroll+extract is WRONG; an advance-only BT is forbidden. `[T:2026-05-19T17:29Z, 2026-06-12T15:02Z; yaml:ARTICLE__READING]`
- **R5.8 First-video user-assist + global player classes.** On a new platform the system MAY ask the user once to identify the video-completion signal; embedded-player families (e.g. YouTube embeds) are a completion-detection class learned ONCE, globally, not per-platform. `[T:2026-06-12T13:13Z, 2026-06-15T13:26Z]`
- **R5.9 Polling states are exempt from exact-hash matching.** Minor tree shifts during polling (timer ticks, ads, player updates) must NOT cause cache misses into fresh classification — polling-state continuity uses its own discipline. Restoration law: any rebuild of the polling state machine MUST cite the archaeology doc for the prior correct implementation; archaeology docs are canonical inputs for any subsystem with a prior correct implementation. `[UNIVERSAL_LAYER_v1:270-281]`

## 6. Actuation rules
- **R6.1** `match_mode` is **exact only**; `contains` is banned. `[mem:no_match_mode_contains, J:2026-05-18]`
- **R6.2** Browsers get `mouse_click`; `ax_press` silently no-ops on Chrome and is banned for browser elements. `[CLAUDE.md Cardinal Rule 10]`
- **R6.3** Composable nodes (`for_each`, `conditional`) take parameters at TOP LEVEL, never inside `params:`. `[CLAUDE.md Cardinal Rule 6]`
- **R6.4** No `fallback` nodes in worker-rebuilt BTs (rejected); `fallback` is legitimate inside deterministic replayed fixed BTs. `[mem:fallback_safe_in_replay_banned_in_rebuild]`
- **R6.5** Selection widgets use their dedicated handler (`select_dropdown_option` owns the strategy ladder + verify); never hand-rolled clicks on menu items; never coordinate-clicks (`click_at`) to open triggers. `[yaml:EXERCISE_DROPDOWN contracts]`
- **R6.6** Presence gates over blind sleeps: `wait_for_element` on the concrete role gates actions on widgets that hydrate/flicker; a naked find after a blind wait is a defect. `[commit bf3ad1d]`
- **R6.7** Invariant safety lives in the ENGINE, not in recipe trust: registered-handler floor, navigate deny-lists — a worker/LLM output can never bypass an engine floor. `[mem:navigate_skip_and_safestop]`
- **R6.8 Navigate is number-based with a hard-filtered candidate set.** The LLM returns the item NUMBER; the server maps it to the exact link name. Course-jumps, skip-links, chrome, footer, and "try again" are REMOVED from the candidate set entirely — the model cannot pick what isn't offered. Exact matching copies the EXACT Unicode (smart quotes included) from the captured tree. `[yaml:NAVIGATION__COURSE_DASHBOARD:22; mem:navigate_skip_and_safestop; _universal.json:23]`
- **R6.9 Coordinates come from the CURRENT capture only.** AX bboxes go STALE after a DOM reorder — drag/click coords derive from the current screenshot/tree, never hardcoded, never reused across sessions/viewports; dynamic element names (that change with state) are never click targets. `[yaml:EXERCISE_SORTER:37, EXERCISE_MATCHER:29, EXERCISE_GRAPH_POINTS:34]`
- **R6.10 No third-party automation frameworks.** Actuation is OS accessibility primitives only — automation tooling is a bot-detection surface. `[T:2026-05-19T16:38Z]`
- **R6.11 In-BT idempotence.** Enumerate/act ONCE per widget per pass; a second pass over already-answered widgets corrupts state (wrapper-dup: operate on the first half only). `[T:2026-05-19T14:11Z; yaml:EXERCISE_DROPDOWN wrapper-dup]`

## 7. Context & truncation
- **R7.1 NO TRUNCATION — EVER, ANYWHERE.** Vectors, text, tokens, payloads: never silently capped or cut. If a budget is hit, whole optional slices are dropped WITH an explicit logged receipt of what and why — or the call refuses and escalates. Change config or escalate; never silently trim. `[mem:no_truncation_ever, J:2026-05-12]`
- **R7.2** Classifier/worker input is filtered by ALLOWLIST (keep content: names/values; drop coords; collapse empty groups) — an unfiltered tree starves the model and produces silent UNKNOWNs. `[mem:tree_filter_starves_classifier; commit 557bfa8]`
- **R7.3** Context is scoped per call (JIT): only the relevant handlers, question types, knowledge slices, and per-screen history for THIS screen — never the full doc dump. `[CLAUDE.md anti-pattern "Send all docs"; J:2026-07-09]`
- **R7.4** Required context missing = refuse and escalate (fail-closed), never freelance under-contexted. `[taey-ed-state-context p4; J:2026-07-09]`
- **R7.5** Every LLM call's served context is receipted (what was included, what was dropped, why) for audit. `[taey-ed-state-context p4]`
- **R7.6 The Mac sends the COMPLETE tree; ALL filtering is server-side.** "It can omit NOTHING! FULL TREE MUST BE INCLUDED AND FILTERED CENTRALLY!!!" Every node, every attribute; ~6MB POST bodies are the accepted cost. The ONE allowed Mac-side check: active-window URL validated against the platform before sending. `[mem:full_tree_no_mac_filtering; commit a8eaaf3; T:2026-06-01T12:22Z]`
- **R7.7 Two-stage server filtering:** global chrome-strip at the boundary ONCE (scope to the active-window web area), then per-screen-type YAML narrowing — never per-consumer re-filtering, and filtering is never truncation (structure dropped, content kept). `[T:2026-06-13T22:49Z; mem:capture_main_pane_not_chrome, tree_filter_starves_classifier]`
- **R7.8 Honest failure over fake data.** A silent zero-vector (or any silently-degraded) fallback is a constitutional violation: on upstream failure return a structured error — the failure surfaces honestly; never fake data shaped correctly. The ONE sanctioned cap: throwaway retrieval QUERY text may be bounded to the embedder's native window — stored content, vectors, and payloads never. `[FORMAL_REBUILD_DISCIPLINE_PLAN:84-85; 02_REFERENCE_FLOW:204-218]`
- **R7.9 Worker context = core rules + THE ONE matching YAML + the screen session. Nothing else.** KB chunks are injected only for exercise-family question types; provisional values are served AS hints with "if it contradicts the tree, trust the tree." Prompts are composed from canonical .md/YAML sources read at runtime — directive text is NEVER hardcoded in Python (no second copy of the rules). `[_SCHEMA.md:4-5; CLAUDE_CLI_WORKER_PROMPT_v1:11-21, 60, 147-149]`
- **R7.10 Worker confinement.** The worker's handoff stages ONLY tree.json + screenshot + system prompt; Read-only tools; cwd-confined; the prompt-size cap RAISES on breach (never silently truncates); supervisor/session text can never reach the worker. `[scr1_adversarial_grok CERTIFY; doc-corpus sweep §8]`

## 8. THE ESCALATION PROCESS (complete, as-built — reconstructed 2026-07-09)
> Full state machine + lifecycle: `dispatches/discovery2/escalation_process.md` (9 states, ~24 transitions, all file:line-cited). The rules below are the binding distillation.

### The ladder
- **R8.1 Single funnel.** Every failure class (worker fallback, wrong answer, not-advanced, stuck, staging-stuck, UNKNOWN, missing artifact, user message) routes through the ONE canonical escalation helper; the Mac app and the API never escalate to the user directly. A user chat message is URGENT: stop everything, escalate immediately, never queue to the worker. `[next_action.py:76-101, 1180-1337, 1351-1376; J:2026-05-18, 2026-06-01]`
- **R8.2 The ladder (definitive):** worker 1 shot per consult → Tier 1 Operator 2 shots (per-screen YAML edit) → Tier 2 Perplexity Deep Research 1 shot → Tier 3 full Family ONE round → terminal. 4 attempts total. The user never performs an escalation. `[escalation.py:44-64; INTENDED_FLOW §D + Decisions #2; commits 47cd946/0f23c3e/4fb1ade; J:2026-06-11]`
- **R8.3 Monotonic, code-owned state.** Attempt count + terminal live in a code-owned store; no flag manipulation advances, resets, or un-terminates the ladder ("62 worker builds on ONE screen" — a severe runaway-cost incident). Terminal is sticky. `[escalation_state.py:1-18, 74-80; commit 0f23c3e; J:2026-06-14]`
- **R8.4 Climb on real attempts only.** The ladder advances once per genuinely DISTINCT failed attempt (deduped by consult id) — never on a timer, never on re-reads of the same failure. Environmental failures (foreground-window mismatch) and infra crashes are NOT attempts. `[escalation_state.py:52-71; commits 97bdaf7, 4fa37d9, dd81663; J:2026-06-14]`
- **R8.5 Exactly three resets:** user-Stop (abandon / session-reset), genuine screen advance, and a YAML fold (recipe edited after the last attempt ⇒ automatic clean ladder — stale failures never carry forward against a fixed program). A fold does NOT un-terminate a terminal screen; only user-Stop/advance do. `[escalation_state.py:83-107; next_action.py:172-192, 1573-1578; consultation.py:128-137; commits f1c718d, 0db433d; J:2026-06-14, 2026-05-20]`

### Consultation lifecycle
- **R8.6 ONE consultation at a time, globally.** A pending consult blocks all new consults; while any consult or diagnosis is open anywhere, every screen waits (global central-feedback lock, 30s poll). Stale pendings self-heal via TTL (600s). `[consultation_request.py:40-88; next_action.py:1191-1302; J:2026-05-18]`
- **R8.7 Worker gets ONE shot and never hangs the Mac.** Any generation failure writes an explicit fallback response, which escalates AND invalidates the consult so the next cycle builds fresh from the current YAML — a cached failure is never re-served. `[consultation_worker.py:93-127; next_action.py:1444-1491; J:2026-05-18]`
- **R8.8 UNKNOWN never reaches the worker.** UNKNOWN means "operator mapping required"; the worker only ever receives a classified screen. `[next_action.py:795-817; commit 557bfa8; J:2026-06-15]`
- **R8.9 Responses are definitions, never answers.** Consult responses are classifications/YAML definitions; the corrected-answer channel to the Mac is disabled; the operator never hands the app a move — the worker building from the YAML is the proof. `[consultation.py:52-56; action_review.py:159-160; tier1 template]`

### Escalation content & dispatch
- **R8.10 Packet completeness, screenshot-first.** Every escalation produces a single-folder packet (packet.md + CURRENT screenshot + CURRENT tree, always-overwritten — never stale) with full attempt history, capabilities snapshot, operational notes. The screenshot is read FIRST — ground truth. `[escalation.py:337-485; next_action.py:50-73, 119-140; J:2026-06-02/06-11]`
- **R8.11 Auto-climb dispatch, server-side.** Tier 2/3 packets are dispatched to taeys-hands BY THE SERVER (Perplexity DR / all-5 Family fan-out with FAMILY_KERNEL + IDENTITY prepends); the operator is a synthesizer, never a relay; responses return verbatim as `response_ready`. Every escalation emission site must actually dispatch. `[escalation.py:79-204; next_action.py:336-353; INTENDED_FLOW §D]`
- **R8.12 Dispatch once per {screen, tier}** — deduped by REVIEWS-file existence + per-tier marker (cleared with the diag dir on advance/user-Stop). `[next_action.py:354-374; operator defect 2026-06-14]`
- **R8.13 Research is never burned on non-screen failures.** Environmental + infra failures SKIP Tier-2/3 dispatch — retry needs the environment/pipeline fixed, not new knowledge. `[escalation.py:106-166; incidents 2026-06-11]`
- **R8.14 Tier-3 is synthesis, not a vote,** with per-member provenance; ONE round only, then terminal. `[escalation.py:392-395, 720-728]`
- **R8.15 Everything from research/Family/user is PROVISIONAL** until a real run on the real screen proves it; failed answers are removed, not kept alongside; 3 validated successes promote. `[INTENDED_FLOW §D-E; tier2/3 templates]`

### Resume
- **R8.16 Resume is code-automatic; humans and AIs touch NO flags.** Per-tier dead-man windows (tier1 180s; tier2/3 1200s) force a fresh attempt if the operator/research stalls; the window RE-TRIGGERS an attempt but NEVER advances the tier. Completion is response-driven in design: response_ready → operator fold → fold-reset gives the re-attempt a clean ladder. `[next_action.py:202-205, 267-283, 1237-1281; commit 01ff53e; J:2026-06-14]`
- **R8.17 Auto-resume abandons the stale consult** so the re-attempt mints a fresh build from the current YAML. `[next_action.py:1262-1277]`

### Terminal & user
- **R8.18 Terminal is the system giving up, never a person.** Ladder exhaustion auto-marks unsolvable, appends `consultations/UNSOLVED.md`, and routes a defect-typed hand-up to the SUPERVISOR (routine tiers → OPERATOR). A terminal screen is a bug report on the system — usually a missing capability; the Supervisor owns the upstream fix. `[next_action.py:224-263; notify_tmux.py:19-30; commit b8a16a4]`
- **R8.19 Terminal user-assist:** the user's dialog carries the operator-authored exact steps/answer verbatim — the user is helped, not just stopped. `[next_action.py:145-170; commit c5c8403; J:2026-06-11]`
- **R8.20 The only real `user_input_needed` states** are terminal screens and dialogs whose answer re-enters as user guidance (one consult); everything else claiming to need the user is intercepted and escalated. `[next_action.py:1180-1337, 1925-1952]`
- **R8.21 Wrong answer = verdict-first, immediate escalation, no reflex resubmit.** A wrong verdict overrides any tree movement; RCA to the determinable cause is required (mastery loss is unrecoverable — prevention is the point). `[next_action.py:884-901, 1623-1672; commit 2f4a245; J:2026-06-14]`

### Image ladder (interleave)
- **R8.22 Image answers climb a deterministic-first ladder inside serving:** platform alt-text (Tier 0, ground truth, free) → screenshot vision → deterministic CV. Engine-injected universally (not per-YAML), relevance-first; complex-enter types (sorter/matcher/graph/label) are exempt from the alt-text shortcut because the answer must still be ENTERED. CV is hard-gated "deterministic-when-proven, escalate-when-not": SHADOW measures + logs + ALWAYS escalates; SUBMIT requires an explicit flag AND CV self-confidence; any refusal feeds the tier ladder as a BT failure. `[next_action.py:671-720; call_gemini.py:165-255; measure_grid.py:1-33; commits 6711685/a846612/b97cd4b/ae88972/f1b1463; J:2026-06-15]`

### Cost & loop discipline
- **R8.23 Anti-runaway floors are ENGINE-owned:** monotonic counter, no timer-climb, ONE-consult gate, ONE TRY ONLY, loop guards (variant=1 fail, staging=3 cycles, hydration=6 polls), dispatch dedup, TTL self-heal, validated-map demote-at-2-never-delete-on-one-off. No recipe, worker output, or operator action may bypass them. `[escalation_state.py:1-18; next_action.py:396-434, 402-411, 1110-1124, 1987-2011]`
- **R8.24 Escalations never bill:** credits debit on successful screen completion only — polls, waits, stuck, retries, wrong-answer escalations do not. `[code.bak/LAUNCH_PLAN.md:91]`
- **R8.25 Knowledge gate:** a platform with no knowledge.json BLOCKS (Perplexity research requested once, flagged, waited) — never build blind on an unknown platform. `[next_action.py:2250-2295]`
- **R8.26 Fleet-ops visibility:** every human-review hold carries a plain-language note in the session's dashboard chat lineage; holds that don't need the human are cleared, not parked; operator-UI messages get a UI reply. `[mem:needs_you_badge_requires_chat_note; J:2026-06-28]`
- **R8.27 Worker-constraint track.** "Solution known but the worker cannot execute it" is a DISTINCT escalation track routed to the operator — a capability gap, not a solve failure; it must not climb the solve ladder. `[T:2026-05-19T16:46Z]`
- **R8.28 Escalation depth floor + no noise.** Chat consults carry the full background (~50K-token order, complete packet), and the operator receives NO meaningless notifications — every notification is actionable. `[T:2026-05-18T22:12Z, 2026-05-19T17:46Z]`
- **R8.29 The full cycle is the only stop.** "You don't stop until you've gone through the full escalation cycle… That is the ONLY time you stop." The user is the last resort only after the complete ladder. `[OP T:2026-06-15T01:39Z]`

## 9. Verification & honesty discipline
- **R9.1 Production is the oracle.** Self-authored synthetic tests are never completion evidence; a real run on the real workload is. `[ORCHESTRATION_INTEGRITY; 6SIGMA_WORKFLOW]`
- **R9.2** End-to-end verification before claiming a fix works: trace every caller + the actual data flow; unit-verifying then declaring done is a violation. `[mem:end_to_end_verification_required, J:2026-05-18]`
- **R9.3** "Done" = evidence (commit SHA + mechanical gate + production observation); honest-incomplete is always acceptable; a false "done" never is. `[ORCHESTRATION_INTEGRITY]`
- **R9.4** Root-cause over patch: a fix that adds a bypass (`if X: continue`) around a broken path must first ask why the path is reachable; the upstream shape is corrected instead wherever possible. First error = full stop + root cause. `[6SIGMA_WORKFLOW; FAMILY_KERNEL behavioral non-negotiables]`
- **R9.5** Confirm an action is right for the STATE, not just that something changed (tree-changed ≠ success; validated advance requires the expected semantic outcome). `[mem:video_complete_is_up_next_present]`
- **R9.6 Operator/Supervisor bright line + cost ladder.** The Operator works the system (YAML edits, ladder climbing, screen mapping) and cannot change it; engines/filters/handlers/flow/skills are Supervisor-only. Cost is a design constraint: automation ≈ free < API worker < supervisor; supervisor involvement is once-per-screen-TYPE, never per-screen. `[INTENDED_FLOW §F; T:2026-06-12T15:52Z, 2026-05-18T14:07Z]`
- **R9.7 Every LLM payload is durably auditable.** Everything sent to any LLM — prompts, trees, screenshots — is saved human-accessible and durable (not /tmp), reviewable by the user. `[T:2026-05-15T12:13/14:26Z]`
- **R9.8 As-designed vs off-script.** Implementing the system as designed/instructed is THE JOB — do it without asking. The forbidden unilateral act is going OFF-SCRIPT: reverting/redesigning what works, contradicting instructions, inventing new approaches mid-task. `[mem:no_unilateral_changes]`
- **R9.9 Repeatable pipeline only.** No session-context one-off prompts; platform knowledge is never typed from an agent's memory — Discovery/Perplexity research populates it through the pipeline; every knowledge.json line traces to a research event + a Step-2-validated run. `[T:2026-05-15T21:27Z; UNIVERSAL_LAYER_v1:583]`
- **R9.10 "Worked" has a formula.** Validated success = tree changed AND new screen ∈ expected_next AND not wrong-answer. "The real screen advancing is the only proof" — never the AI's say-so. `[UNIVERSAL_LAYER_v1:627; INTENDED_FLOW:28,36]`
- **R9.11 Auditable intention.** Every worker BT response carries structured justification: target_source (exact text copied from tree/screenshot), why_safe, never_clicks_avoided, confidence — so reasoning is verifiable BEFORE execution. `[UNIVERSAL_LAYER_v1:395-411]`
- **R9.12 Rebuild discipline (binding):** no stubs; no TODO markers in shipped code; no inline LLM-directive strings in Python; no platform-specific Python conditionals; no "minimum-viable" scope cuts; no time estimates; cannot-lie provenance — every "working" claim points to a verifiable artifact. `[FORMAL_REBUILD_DISCIPLINE_PLAN:9-17]`
- **R9.13 The Supervisor monitors and scopes the Operator.** Context-scoping is the enforcement mechanism: the Operator's instructions/skills are scoped to exactly its job so off-script options don't exist; the Supervisor watches for drift and adjusts instructions/flow/context — up-handoff is terminal-or-needs-system-change, down-handoff is revised instructions. `[INTENDED_FLOW:78-102]`
- **R9.14 Human gates are abolished.** "The only valid gates are live production runs and full-code Family audits" — no approval checkpoint may park work on a human; gates are evidence, not sign-off. `[recap 2026-06-11; J delegation 2026-07-09]`

## 10. Knowledge & learning
- **R10.1** Working solutions are never destroyed — they are superseded with history (versioned BTs, supersession chains); regressions must be recoverable by construction. `[J:2026-07-03 "You lose everything positive or destroy it"]`
- **R10.2** Solved screens persist: identity + BT + Q/A capture survive restarts and sessions; the system never re-derives what it already solved. `[J:2026-07-03/07-09]`
- **R10.3** Platform knowledge files stay human-readable and canonical on disk; any DB layer over them is a rebuildable index, never the source of truth. `[taey-ed-state-context p1-schema-design]`
- **R10.4** Learned observations feed future prompts for the same screen type; learning writes are bounded and summarized, never unbounded growth. `[CLAUDE.md learning loop]` *(NOTE: `learned/` read path removed as dead 2026-07-09; write side + files decided at p2-importer.)*
- **R10.5 Provisional → trusted lifecycle.** Platform research runs UP FRONT (not inline, not failure-driven); findings are stored PROVISIONAL and graduate to trusted ONLY by surviving real validated successes on real screens — piece-level (only what was actually used and proven), never whole bundles. **Three validated successes promote** a screen type to trusted/instant handling; failures demote; trusted knowledge is never silently overwritten. Verification is part of the writing process: a note is verified by observing it actually work, and its verified state reflects real observation — never an artificial default in either direction. `[INTENDED_FLOW §B/E + Decisions #3/#5; T:2026-05-18T20:47Z — adjudicates the verified_count conflict]`
- **R10.6 One YAML per (platform, screen type) = the complete, current, one-page truth.** Learning EDITS THE YAML IN PLACE — supersede, never layer; a type's YAML can never contain two conflicting instructions. `[_SCHEMA.md:3-10]`
- **R10.7 Only the operator names screens** — the worker never defines screen names/types. Signatures are built from static/semi-static features only (never dynamic content); zero-or-multiple signature matches → UNKNOWN path; multiple matches flag the operator to tighten. `[T:2026-05-19T15:11/15:17Z]`
- **R10.8 Onboarding UX:** Taey greets the user on a first-touch platform and sets the expectation — "I might need your help on the first video and first screen, but after that I'll be good on my own." Minimal, front-loaded, then autonomous. `[T:2026-05-15T21:38Z; INTENDED_FLOW §A.2]`

## 11. Product / trust / billing (LAUNCH_PLAN — binding for launch)
- **R11.1 MVP exit gates (all five):** stranger signs up/pays/logs in without touching files; every request tied to a real user_id (no shared keys); credits debit exactly once per successful billable unit — never retries/polls/waits/failures; the app is honest (shows what it's doing, why waiting, how to stop; humanized failure copy; never silently retry-spams or charges incomplete work); user content movement is auditable (local KB default, app says what leaves the machine). `[LAUNCH_PLAN:31-35]`
- **R11.2 Billing:** append-only ledger, never a decremented counter; idempotency key = user+session+directive+screen-hash-before/after; "credits buy completed screens, not attempts." `[LAUNCH_PLAN:80-93]`
- **R11.3 Ingress hardening (blocking, pre-tunnel):** no default keys/secrets in production; docs endpoints protected; CORS restricted; body-size limits; per-user/IP rate limits; only health public; **never log screenshots, tokens, full prompts, full content, or answer payloads**; backups on. `[LAUNCH_PLAN:59-68, 209]`
- **R11.4 KB user controls:** "use local course memory" toggle; "show retrieved context before answer" option; per-course delete; top-K ≤ 5, never the whole KB. Worker-queue floor: concurrency cap, per-user rate limit, per-job timeout, max prompt size, output schema validation, cost logging, automatic user_input_needed fallback. `[LAUNCH_PLAN:120-145]`
- **R11.5 Trust UX:** immediate Stop; on stop+resume re-capture and re-ask the server (never assume an old directive valid); failure copy names what happened and the user's three options (guide / skip automation / stop). `[LAUNCH_PLAN:178-182]`

## 12. Open items awaiting ruling
- **O1** `learned/` store: revive read path vs delete (R10.4 note). `[inventory 1.6]`
- **O2** Worker direct-solves vs prescribed find_all→send_to_llm→for_each recipe shape (recipe-design question). `[mem:worker_bt_shape_variance OPEN]`
- **O3** Legacy transition variant_bts consolidation. `[mem:transition_deterministic_serve_cluster OPEN]`
- **O4** `spark_v2/` in the codex worktree: generated debris — deletion CONFIRMED (removed-as-unused per commit 2c82ab0), executing in cleanup-repo-hygiene. `[codex step-0 report 2026-07-09]`
- **O5** Abandon-clear hash-key mismatch: `/abandon_consultation` clears escalation state by `compute_tree_hash` while the live ladder keys by `skeleton_hash` — the per-consult user-Stop reset is likely a NO-OP (masked by `/session/reset`). Fix in p2 coordination migration. `[escalation reconstruction F1]`
- **O6** Dormant duplicate ladder (`consultation_request.py:255-431`, retries.txt path) bypasses the monotonic store if ever re-reached; no repo caller sets its trigger. Delete in p2 flag-machine migration. `[F2]`
- **O7** CLAUDE.md "Escalation Model" section is two generations stale (3-failed-consults / spark_attempts≥2 → superseded by one-shot + 4-tier ladder); "Gemini decides"/Gemini-primary wording predates the Claude worker cutover. Rewrite CLAUDE.md against this canon. `[F3; git mine]`
- **O8** Empty-consult-id escalations (stuck/UNKNOWN/user-message) bypass note_attempt dedup — each resume-cycle failure legitimately climbs; bounded but imperfect. Model properly in the p2 coordination schema. `[F4]`
- **O9** No code path extends `resume_at` while research is in flight — a DR outlasting the 20-min window can climb the ladder indirectly through real re-attempt failures. Design a response-ready-gated resume in p2. `[F5]`
- **O10** "GEMINI GONE… removed from code entirely" (J:2026-05-18, thrice; UNIVERSAL_LAYER "Sole LLM: Claude") vs `call_gemini.py` still live (measure_grid glue) and CLAUDE.md still naming Gemini primary. Reconcile in p5 + the CLAUDE.md rewrite (O7): rename/refactor the module to its real remaining role or knowingly amend the ruling. `[chat mine #5; corpus C7]`
- **O11** Promotion math: Jesse's 3-validated-successes (06-11, canon R10.5) superseded the v1 spec's threshold (5 + CI>0.95 + 2 courses + cleanliness) — whether the CI/cleanliness/cross-course conditions survive ALONGSIDE "3" is unreconciled. Decide in p2 schema (promotion counters). `[corpus C3]`
- **O12** Mac-side `MAX_CONSULTATION_STEPS=3` (Feb-era, frozen app) vs the server-owned 4-attempt ladder — whether the Mac constant still gates anything is Unknown; verify on the next live run. `[corpus C11]`
