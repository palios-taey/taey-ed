__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.db
*.sqlite
/tmp/
.env
palios-taey-secrets.json
*.log
.DS_Store
.pytest_cache/
venv/

# Learned data — runtime-generated, not committed
spark/platforms/*/learned/
.gitnexus

# venv (we use .venv/ on Mira side)
.venv/

# Jupyter checkpoint cruft
.ipynb_checkpoints/

# Mirrored consult artifacts
runtime/

# Runtime escalation / consult dumps (large, regenerated each run)
consultations/ESCALATIONS/
logs/
*.pre_migration_bak
/tmp/taey-ed-mac-raw-dumps/

# Runtime consultation / review artifacts (Family review dumps, unsolved log,
# corpus) — process output, regenerated each run, not part of the published code.
consultations/REVIEWS/
consultations/UNSOLVED.md
consultations/*.jsonl

# Internal fleet/agent + planning docs — kept on disk for the live fleet
# (project context, escalation-prompt references), NEVER published. Removed from
# the public repo on 2026-06-15; gitignored so they can't be re-tracked.
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/app/CLAUDE.md
/INTENDED_FLOW.md
/LAUNCH_PLAN.md
/PLAN_SMART_CONTEXT_REBUILD.md
/PLAN_SUBTYPE_ARCHITECTURE.md
/TRANSITION_smart_context_rebuild.md
/dispatch/
/audits/
/plans/

# 2026-07-09: state-context rebuild internal docs — on-disk for the fleet, not published.
/PLAN_STATE_CONTEXT_REBUILD.md
/dispatches/
/.claude/
/taey35b_verify_responses/
