platform: khan_academy
version: "15.0"

# V15: All screen handling via Gemini + dynamic knowledge (knowledge.json).
# No hardcoded BT templates. Deterministic BTs built by Gemini and cached
# in screen signatures. config.yaml retained for platform identity only.

screens: {}
